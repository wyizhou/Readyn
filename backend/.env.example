# Garmin 中国区（connect.garmin.cn）账号凭证。
# 复制为 backend/.env 并填入真实值；.env 已被 .gitignore 忽略，不会提交。
# 首次 /api/garmin/connect 登录后会缓存 OAuth 令牌，后续 /api/garmin/sync 无需密码。
GARMIN_CN_EMAIL=
GARMIN_CN_PASSWORD=
# 区域域名，默认中国区；如需国际区改为 garmin.com
GARMIN_DOMAIN=garmin.cn
